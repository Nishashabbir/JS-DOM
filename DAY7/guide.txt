The proper sequence is:

Synchronous code
Asynchronous code
Callbacks
Callback hell
Promises
async/await



Example:

console.log("A");
console.log("B");
console.log("C");

Output:

A
B
C

Simple.

But some tasks take time

Example:

downloading data
reading a file
talking to database
API requests

These are called asynchronous tasks.

JavaScript does NOT stop everything and wait.

Instead:

“Start the task, continue other work, and tell me when done.”

Example of async behavior
console.log("Start");

setTimeout(function() {
  console.log("Hello after 2 seconds");
}, 2000);

console.log("End");

Output:

Start
End
Hello after 2 seconds

Notice:

setTimeout did NOT block the program.

So how does JS know WHEN the task finishes?

Using callbacks.

setTimeout(function() {
  console.log("Done");
}, 2000);

This function:

function() {
  console.log("Done");
}

is the callback.

Meaning:

“Run this later.”

Then callback hell appears

When async tasks depend on previous async tasks.

Example:

getUser(function(user) {

  getOrders(user.id, function(orders) {

    getOrderDetails(orders[0], function(details) {

      console.log(details);

    });

  });

});

Each step waits for previous step.

So nesting grows.

That’s callback hell.

NOW comes the next evolution: Promises
Developers got tired of deep nesting.
So JavaScript introduced:

Promise

A Promise is basically:
“I promise to give you a value later.”

Instead of:

getUser(function(user) {

});

you can do:

getUser()
  .then(function(user) {

  });

Cleaner.

THEN comes await

Now finally we can talk about it properly.

await is built on top of Promises.

It makes async code LOOK synchronous.

What await does

This:

const user = await getUser();

means:

“Pause THIS function until getUser() finishes.”

Not the whole program.
Only that async function pauses.

Important rule

You can ONLY use await inside an async function.

Example:

async function loadData() {

  const user = await getUser();

  console.log(user);

}
Compare all 3 generations
1. Callback style
getUser(function(user) {
  console.log(user);
});
2. Promise style
getUser()
  .then(function(user) {
    console.log(user);
  });
3. async/await style
async function run() {

  const user = await getUser();

  console.log(user);

}
Why developers love await

Because it removes pyramid structure.
Instead of:

getUser(function(user) {
  getOrders(user.id, function(orders) {
    getDetails(orders[0], function(details) {

    });
  });
});

You write:
const user = await getUser();
const orders = await getOrders(user.id);
const details = await getDetails(orders[0]);

Flat.
Readable.
Easy to debug.

MOST IMPORTANT CONCEPT
await does NOT magically make things synchronous.
The task is STILL asynchronous.

await only makes the code LOOK cleaner and easier to write
What is an async task?

An async task is something that takes time:

fetching data from server
reading a file
waiting 2 seconds
database query
JavaScript says:
“I’ll continue running other code while this task finishes.”

///////////////////////////////////
promises 
Promise style
getUser()
  .then(function(user) {
    console.log(user);
  });

Now:

getUser() immediately returns a Promise object
YOU control what happens next using .then()

This is a cleaner contract.

3. Your confusion:
“But .then(function(){}) is ALSO a callback!”

YES.
100% correct.

.then() STILL receives a callback function.

So promises did NOT remove callbacks completely.

This is VERY important.

Promises are actually:

A better system BUILT ON callbacks
4. Then what problem did Promise solve?

The problem was NOT:
 “waiting”

The problem was:
 nested pyramid structure
 messy error handling
 losing control
 callback chaos

5. Compare carefully
Callback nesting
getUser(function(user) {

  getOrders(user.id, function(orders) {

    getDetails(orders[0], function(details) {

      console.log(details);

    });

  });

});

Notice:
Every next step is INSIDE previous step.

This creates the “pyramid”.

6. Promise chaining
getUser()
  .then(function(user) {
    return getOrders(user.id);
  })
  .then(function(orders) {
    return getDetails(orders[0]);
  })
  .then(function(details) {
    console.log(details);
  });

Now each step is separated.
No pyramid.
That’s the big improvement.

7. MOST IMPORTANT MAGIC
This line:
return getOrders(user.id);
is the entire magic of promises.
Why?
Because:
If you return a Promise from .then(),
the NEXT .then() waits automatically.
Callbacks could not do this naturally.
8. Let’s understand Promise creation VERY slowly

You asked:
“why resolve reject?”
Good.
9. Promise syntax
const p = new Promise(function(resolve, reject) {

});
When you create a Promise:
JavaScript gives you TWO functions automatically:
resolve -> success
reject  -> failure

You don’t create them yourself.
JavaScript gives them to you.


/////////////////////////////////////////////////////////////////////
1. Is promise a keyword?

Look carefully:

const promise = new Promise(...)

Here:

promise

is JUST a variable name.

You could write:

const banana = new Promise(...)

or:

const myFutureData = new Promise(...)

and it would still work.

 BUT THIS PART IS SPECIAL

THIS is the actual built-in thing:

Promise

with capital P.

That is JavaScript’s built-in Promise constructor.

So:

Thing	Meaning
promise	normal variable
Promise	built-in JS class/constructor
 Same idea happens everywhere
const arr = new Array();
const date = new Date();
const user = new User();

Array, Date, Promise
→ built-in constructors/classes

while:

arr
date
user
→ YOUR variables
 SECOND QUESTION — VERY IMPORTANT

You asked:
“after fetchData we didn’t pass data function inside it”

YES.

And this is where promises are fundamentally different from callbacks.

 CALLBACK STYLE

In callbacks:

fetchData(function(data) {
  console.log(data);
});

You directly PASS a function INTO fetchData.

So:

fetchData receives your callback
fetchData decides when to run it
 PROMISE STYLE

Now look carefully:

fetchData().then(function(data) {
  console.log(data);
});

THIS is different.

 What does fetchData() return?

It returns:

a Promise object

NOT data immediately.

So:

fetchData()

becomes something like:

Promise { pending }

initially.

 THEN WHAT IS .then() DOING?

.then() is a method that belongs to Promise objects.

Like:

array.push()
string.toUpperCase()

Similarly:

promise.then()
 IMPORTANT MENTAL MODEL

This:

fetchData().then(callback)

means:

“Hey Promise object… when you finally finish and get the result… THEN run this callback.”

THAT’S why it is called:

.then()

As in:

first finish operation… THEN do this.

 THIS IS MASSIVE

Promises STILL use callbacks internally.

People think promises “replace callbacks”.

Not exactly.

They organize them better.

Because:

.then(data => {})

is ALSO a callback function.

But:

cleaner
chainable
manageable
avoids pyramid doom
 Let’s compare side-by-side
CALLBACK VERSION
fetchData(function(data) {
  console.log(data);
});

You GIVE callback to function.

PROMISE VERSION
fetchData()
  .then(function(data) {
    console.log(data);
  });

You attach callback to Promise object.

 This is the TRUE mental shift

Callbacks:
Function controls callback
Promises:
Promise object controls future result

That distinction is HUGE.
 INTERNAL FLOW (very important)
When this runs:
fetchData()
  .then(data => console.log(data));

JS internally does something LIKE:

1. fetchData starts async work
2. fetchData immediately returns Promise object
3. Promise is pending
4. .then registers callback
5. async work finishes
6. Promise resolves
7. registered callback executes

That is REAL async understanding.

Not tutorial memorization.
 Another Important Detail
This:
.then(data => {})
does NOT run immediately.
It gets stored by the Promise system until:
resolve happens
OR
reject happens

//////////////////////////////////////////////

when to use what ??
RULE #1 — resolve and reject

You ONLY use:

resolve
reject

INSIDE:

new Promise(...)
Because THEY create the promise result.

 RULE #2 — .then() and .catch()
You use:

.then()
.catch()

AFTER a promise already exists.
Because THEY consume the promise result.

 THIS IS THE FULL FLOW
const promise = new Promise((resolve, reject) => {
  resolve("Success");
});

promise.then(data => {
  console.log(data);
});
 VISUAL UNDERSTANDING
PART 1 — CREATE PROMISE
new Promise((resolve, reject) => {})
This creates the “future box”.

PART 2 — FINISH PROMISE
resolve(data)

OR

reject(error)

This settles the promise.

PART 3 — USE PROMISE
.then()
.catch()
This listens for the future result.

 REAL PROGRAM 1 — Fake Login System

This is VERY realistic beginner backend/frontend logic.

STEP 1 — CREATE PROMISE
function login(password) {

  return new Promise((resolve, reject) => {

    setTimeout(() => {

      if (password === "1234") {
        resolve("Login successful");
      } else {
        reject("Wrong password");
      }

    }, 2000);

  });

}
 BREAKDOWN
THIS PART:
new Promise(...)
creates promise object.
THIS PART:
(resolve, reject) => {}

defines:
success handler
failure handler
THIS PART:
resolve("Login successful")
means:
 success happened
THIS PART:
reject("Wrong password")

means:
 failure happened

STEP 2 — USE THE PROMISE
login("1234")
  .then(message => {
    console.log(message);
  })
  .catch(error => {
    console.log(error);
  });
 IMPORTANT UNDERSTANDING

This:
login("1234")
returns:
Promise object
NOT actual data immediately.

That’s why we use:

.then()
 REAL PROGRAM 2 — Simulated API Fetch
CREATE
function fetchUser() {
  return new Promise((resolve, reject) => {

    setTimeout(() => {

      const success = true;

      if (success) {
        resolve({
          id: 1,
          name: "Nishu"
        });
      } else {
        reject("Server error");
      }

    }, 3000);

  });

}
USE
fetchUser()
  .then(user => {
    console.log(user.name);
  })
  .catch(error => {
    console.log(error);
  });
 WHY SOMETIMES YOU DON’T SEE new Promise
EXCELLENT question.
Because MANY built-in functions ALREADY RETURN PROMISES.

Like:

fetch()
Example
fetch("https://api.com/users")
  .then(response => response.json())
  .then(data => console.log(data));

Here:

YOU did not create promise
fetch() already created and returned one
So you only consume it using:

.then()
 HUGE UNDERSTANDING
YOU USE new Promise
When:
 YOU are building async functionality

Examples:
database wrapper
timer
custom async task
authentication simulation
YOU USE .then()

When:
 you are USING an already existing promise
Examples:

fetch()
axios
database calls
login systems
async functions
 REAL WORLD PROFESSIONAL VIEW

Most developers:
USE promises daily
CREATE custom promises less frequently
Because APIs/frameworks already provide promises.
 REAL PROGRAM 3 — Delay Function
This is VERY important professionally.
function delay(ms) {

  return new Promise(resolve => {

    setTimeout(() => {
      resolve();
    }, ms);

  });

}
USE
delay(2000)
  .then(() => {
    console.log("2 seconds passed");
  });
 THIS IS HUGE

You just converted:

setTimeout

into a promise-based system.

Professional developers do this kind of thing ALL the time.

 SIMPLE MEMORY RULE
WHEN CREATING ASYNC WORK
Use:
new Promise((resolve, reject) => {})
WHEN SUCCESS HAPPENS
Use:
resolve(data)
WHEN FAILURE HAPPENS

Use:
reject(error)
WHEN USING RESULT
Use:

.then()
.catch()


//////////////////////////////////// ASYNC AWAIT 

NOW YOU’RE READY FOR THE BIG EVOLUTION
Everything so far was to prepare for THIS:

 async / await
This is where JS stops looking like:

.then().then().then()
and starts looking like:
const user = await getUser();

Which feels synchronous…
but secretly still uses promises underneath.
 FIRST IMPORTANT QUESTION
What do YOU think this does?

async function test() {
  return 5;
}
Does it return:

number 5?
promise?
undefined?

Think carefully before answering.
This single question unlocks async/await deeply.
promise to return 5 

YES. 
That answer is VERY important.
 THIS IS THE GOLDEN RULE
An async function ALWAYS returns a Promise.
Even if you write:
return 5;

JS secretly converts it into:
return Promise.resolve(5);
 So this:
async function test() {
  return 5;
}

is basically equivalent to:
function test() {
  return Promise.resolve(5);
}
 THAT IS MASSIVE
Because now:

test()
returns:
Promise
NOT directly 5.
 So how do we get the 5?

Using:
.then()
like:
test().then(value => {
  console.log(value);
});

Output:

5
 IMPORTANT CONNECTION
Now notice something beautiful:
Before:
new Promise(...).then(...)
Now:
async function + .then(...)
Same promise system underneath.
 NOW ENTER await
This is where JS becomes readable.

 Example
function getUser() {
  return new Promise(resolve => {
    setTimeout(() => {
      resolve("Ali");
    }, 2000);
  });
}
OLD STYLE:
getUser().then(user => {
  console.log(user);
});
 ASYNC/AWAIT STYLE
async function displayUser() {
  const user = await getUser();
  console.log(user);

}
 WHAT DOES await DO?
This is the REAL meaning:
await getUser()
means:
“Pause this async function until promise finishes.”

 IMPORTANT
It DOES NOT block entire JavaScript.
It only pauses:
that async function
Huge difference.
 INTERNAL MAGIC
This:
const user = await getUser();
is basically:
getUser().then(user => {
  const userData = user;
});
So:
await is syntax sugar over promises.
 WHY DEVELOPERS LOVE IT
Compare:
PROMISE CHAINING
getUser()
  .then(user => getOrders(user.id))
  .then(orders => getOrderDetails(orders[0]))
  .then(details => console.log(details));
ASYNC/AWAIT
async function run() {

  const user = await getUser();
  const orders = await getOrders(user.id);
  const details = await getOrderDetails(orders[0]);
  console.log(details);
 NOTICE SOMETHING HUGE

Now it reads:
top to bottom
like normal code.
That’s why async/await changed JavaScript forever.
 IMPORTANT RULE
You can ONLY use:
await
inside:
async function
 NOW YOUR FIRST EXERCISE
No hints this time 
 TASK
Create:
function getNumber()
It should:
return a promise
after 2 seconds resolve 10
Then create:

async function showNumber()
Inside it:
use await
store result in variable
print it
Then call:
showNumber()


//////////////////////////////////////////////////////

JavaScript
promise
  .then((data) => {
    console.log(data);
  })
  .catch((error) => {
    console.log(error);
  });
Python equivalent mindset
try:
    result = something()
    print(result)

except Exception as error:
    print(error)

So mentally:

JavaScript	Python
.then()	try success part
.catch()	except
resolve()	returning success
reject()	throwing an error
BUT there’s a deeper twist 🧠

.then() / .catch() are mainly for Promises (asynchronous work).
Python’s normal try/except is not automatically async.
Does Python have async/await?

YES. Absolutely.
Python has:
async def
await

Very similar to JavaScript.

JavaScript async/await
async function test() {
  try {
    const result = await checkAge(20);
    console.log(result);
  } catch (error) {
    console.log(error);
  }
}
Python async/await
import asyncio

async def test():
    try:
        result = await some_function()
        print(result)
    except Exception as error:
        print(error)

See the similarity?
JavaScript and Python developers secretly copy homework from each other all the time 😭
Important difference
In JavaScript
async function hello() {}
automatically returns a Promise.
In Python:
async def hello():
returns a coroutine object.
To actually run it, Python usually needs:
asyncio.run(hello())
Otherwise Python just stares at the coroutine like:
“Cool function bro. You gonna run it or frame it on the wall?” 😭